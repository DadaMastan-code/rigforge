/**
 * Shared cubic-bezier easing (expo-out). Typed as a 4-tuple so framer-motion's
 * `Easing` type accepts it (a bare number[] is rejected).
 */
export const EASE: [number, number, number, number] = [0.16, 1, 0.3, 1]
